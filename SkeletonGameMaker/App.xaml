<Application x:Class="SkeletonGameMaker.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:SkeletonGameMaker"
             Startup="Application_Startup">
    <Application.Resources>
        <ControlTemplate x:Key="ButtonMenuLarge" TargetType="Button">
            <Grid>
                <Border BorderBrush="Black" BorderThickness="0" Background="#baeeff">
                    <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding Content, RelativeSource={RelativeSource TemplatedParent}, StringFormat=0}"/>
                </Border>
            </Grid>
        </ControlTemplate>
    </Application.Resources>
</Application>
